# Kubernetes
Check the [documentation](../documentation/microsoft_azure_deployment.md) for a guide on how to deploy in Microsoft Azure.

The `minimal.yaml` contains a lightweight version of the project, while the `kubernetes.yaml` deploys the full project and requires some more resources.
